// Make me into a typescript file and Assign from app param to HTMLElement

/**
 * @param {HTMLElement} app
 */

export function appendTitle(app) {
	app.innerHTML = `<h1>Welcome to an introduction to JavScript and Typescript!</h1>`
}
